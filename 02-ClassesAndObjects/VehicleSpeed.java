public class VehicleSpeed {
    public static void main(String[] args) {
        int speed = 141;
        boolean isValid = speed>=40 && speed<=140;
        System.out.println("Vehicle speed: " + speed + "\n" + "Speed is valid: " + isValid);
    }
}
